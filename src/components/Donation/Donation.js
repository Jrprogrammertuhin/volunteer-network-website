import React from 'react';

const Donation = (props) => {
    console.log(props)
    return (
        <div>
            <h1>This is donation page </h1>
        </div>
    );
};

export default Donation;